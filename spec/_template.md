---
doc_id: "{{PROJECT}}-{{SCOPE}}-SPEC-{{NNNN}}"
title: "{{対象}}の仕様"
type: spec
status: Draft
owners: ["{{担当者またはチーム}}"]
updated: "{{YYYYMMDD}}"
# 独立した仕様の版を公開するときだけ有効化する。
# rev: "0.1.0"
# 既存の上位要件があるときだけ指定する。要件台帳を新設する必要はない。
# trace:
#   req: ["{{上位要件ID}}"]
---

# {{対象}}の仕様

<!--
コピー先: docs/spec/<doc_id>-<固定slug>.md
{{...}}を置換し、この説明コメントは削除する。
必須: 要約と適用対象、スコープ、要件と検証、契約、関連する判断、未決事項の有無。
内部設計・運用は関係する事項だけ残し、不要な任意節は削除する。
ここに書くのは合意対象となる仕様であり、Activeになった時点で合意済みの契約として扱う。実装済みであることの証明ではない。
-->

## 要約・適用対象

{{誰の何の問題を、何によって解決するかを2〜3文で書く。}}

- 適用対象: {{コンポーネント、コードの場所、対象リリースまたはブランチ}}
- 外部との境界: {{呼び出し元、依存先、実行環境}}
- 実装との差分: {{なし／未実装・移行中の要件と追跡先。進捗一覧はIssue等に置く}}

## スコープ・非スコープ

対象: {{この仕様が保証する責務・機能。}}

対象外: {{保証しないこと、別コンポーネントの責務、今回先送りすること。}}

前提・制約: {{環境、外部契約、制約とその確認先。未検証の前提は明示する。}}

## 要件と受け入れ条件

<!--
Fは機能、Nは非機能。Draftを含め、最初にマージまたは外部参照された時点でIDを固定し、廃止後も再利用しない。
要件行の状態はCurrentまたはDeprecated。CurrentはそのSPECの現行内容であることを表し、SPEC全体がDraftならまだ合意済みではない。
廃止した要件は行を削除せず、状態をDeprecatedとし、廃止理由と後継要件または代替先を受け入れ条件欄に残す。
外部からの識別は「<doc_id>/F001」。これは論理IDであり、URLのアンカーではない。
要件は1行1義務とし、主体・成立条件・求める振る舞いを明記する。「適切に」「速やかに」等の測定不能な表現を単独で使わない。
受け入れ基準には合否を判定できる条件だけを書き、検証参照にはテスト、CI、解析等の成果物へのリンクまたは計画を書く。実行結果自体はSPECへ転記しない。
同じ要件を別のテスト計画書やトレーサビリティ表に転記しない。
下の行は記入例。該当しない分類の行は削除し、必要な要件だけを追加する。
-->

| ID | 状態 | 規範要件 | 受け入れ基準 | 検証参照 |
|---|---|---|---|---|
| F001 | Current | {{主体が、どの入力・状況で何を保証するか}} | {{入力と観測可能な合格条件}} | {{テスト、CI、解析等の参照先または計画}} |
| F002 | Current | {{不正入力・失敗・境界条件で何をするか}} | {{条件と期待する結果}} | {{検証の参照先または計画}} |
| N001 | Current | {{性能・可用性等の測定可能な要求}} | {{開始点・終了点、負荷、測定期間、集計方法、閾値}} | {{負荷試験等の参照先または計画}} |

## 外部契約・振る舞い

### 入出力・データ

{{API、CLI、公開関数、ファイル、イベント等の境界と責務を書く。}}

正本: {{OpenAPI、JSON Schema、公開ヘッダー等があればその実在する場所へリンクし、なければ「なし」と書く。}}

{{型、必須項目、単位、値域、エラー、互換性。正本にある定義は全文を複製しない。}}

### 状態・失敗時の扱い

{{関連する事項だけ記載する: 状態遷移、不変条件、順序、並行性、タイムアウト、
キャンセル、再試行、重複、部分成功、永続化、再起動、リソース上限。}}

{{成功だけでなく、何が保証されないかも書く。}}

### 安全性・信頼境界（必要な場合）

{{どこを信頼するか、認証・認可の担当、不正入力時の扱い、秘匿情報の扱い、
危険な操作の制限と監査。詳しい方針が別にあれば参照する。}}

## 内部設計の要点（任意・非規範）

{{実装を理解するために必要な責務分担、データの流れ、重要な不変条件だけを書く。}}

{{クラスや関数の全件説明、コードの逐語訳は書かない。}}

## 運用上の契約（必要な場合）

{{起動・停止の成立条件、設定変更の反映、ヘルス判定、監視、復旧、移行・互換性。}}

{{短い手順はここに置く。独立した作業として参照される場合だけrunbookへ分ける。}}

## 設計判断・関連資料

{{現在の方針を一文で書き、根拠となるADRへ相対リンクする。ADRが不要なら短い理由を書く。}}

<!-- リンク例: [{{ADRのdoc_id}}](../adr/{{実ファイル名}}.md) -->

{{用語集、実装、テスト、Issue・PR等への必要最小限のリンク。}}

## 未決事項

{{なければ「なし」。ある場合は下の表を使う。実装タスク全体の一覧にはしない。}}

ActiveのSPECに残せるのは、現在の保証・適用範囲・受け入れ条件を変えない非阻害事項だけとする。それらを確定できない未決事項がある間はDraftとする。

| 論点 | Activeを阻害するか | 担当 | 決定条件・期限 | 影響・追跡先 |
|---|---|---|---|---|
| {{未決事項}} | {{Yes / No。ActiveではNoのみ}} | {{担当}} | {{何が分かれば／いつまでに決めるか}} | {{実装を止める範囲、Issue等}} |
